import styled from 'styled-components';

const Card = styled.div`
    border-radius: .5rem;
    display: flex;
    position: relative;
    flex-direction: column;
    margin-bottom: 1.5rem
`;

export default Card;